export const ROLES = Object.freeze({
  Admin: 'ROLE_ADMIN',
  Editor: 'ROLE_EDITOR',
  User: 'ROLE_USER',
})
export const DATA_UI_MODE = Object.freeze({
  Default: 'default',
  Map: 'map',
})

export const DATA_FORM_MODE = Object.freeze({
  Create: 'create',
  Read: 'read',
  Update: 'update',
  Delete: 'delete',
})

export const API_VALIDATOR_TYPE = Object.freeze({
  Unique: 'unique',
})
